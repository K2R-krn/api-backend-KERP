# api-backend-KERP

API backend for a multi-branch fertilizer-shop ERP (replacing TallyPrime), built with Node, TypeScript, Express, and Prisma; run `npm install`, copy `.env.example` to `.env` and fill in real values, then `npm run dev` to start the server in watch mode against `GET /health`.
